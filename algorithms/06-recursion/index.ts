export type Point = {
    x: number;
    y: number;
}

export default function solve(
    maze: string[],
    wall: string,
    start: Point,
    end: Point,
): Point[] {
    // TODO: Implement the algorithm here.
    return [];
}
